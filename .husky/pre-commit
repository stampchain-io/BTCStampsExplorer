echo "🔍 Running security checks..."

# Check for potential secrets and sensitive information
check_secrets() {
    local files="$1"
    local found_issues=false
    
    echo "Checking for potential secrets..."
    
    # API keys and tokens - use simpler patterns
    if echo "$files" | grep -qE "api.*key|secret|token|password|auth" | head -20; then
        echo "⚠️  Potential secret patterns found:"
        echo "$files" | grep -E "api.*key|secret|token|password|auth" | head -20 | sed 's/^/   /'
        found_issues=true
    fi
    
    # Common secret patterns - use simpler patterns
    if echo "$files" | grep -qE "sk_|pk_|ghp_|gho_|ghu_|ghs_|ghr_|github_pat_" | head -20; then
        echo "⚠️  Potential API key patterns found:"
        echo "$files" | grep -E "sk_|pk_|ghp_|gho_|ghu_|ghs_|ghr_|github_pat_" | head -20 | sed 's/^/   /'
        found_issues=true
    fi
    
    # Personal directory paths - use simpler patterns
    if echo "$files" | grep -qE "/Users/|/home/|C:\\\\Users" | head -20; then
        echo "⚠️  Personal directory paths detected:"
        echo "$files" | grep -E "/Users/|/home/|C:\\\\Users" | head -20 | sed 's/^/   /'
        found_issues=true
    fi
    
    # Local config files - use simpler patterns
    if echo "$files" | grep -qE "\.env|config\.local|\.local" | head -20; then
        echo "⚠️  Potential local config file references:"
        echo "$files" | grep -E "\.env|config\.local|\.local" | head -20 | sed 's/^/   /'
        found_issues=true
    fi
    
    if [ "$found_issues" = true ]; then
        echo ""
        echo "❌ Security check failed! Please review the flagged content above."
        echo "   If these are false positives (error messages, legitimate code, etc.),"
        echo "   you can commit with: git commit --no-verify"
        echo ""
        return 1
    fi
    
    echo "✅ No security issues detected"
    return 0
}

# Get staged files
staged_files=$(git diff --cached --name-only)

if [ -z "$staged_files" ]; then
    echo "No staged files to check"
    exit 0
fi

# Get file contents for security check
file_contents=$(git diff --cached)

# Run security checks
if ! check_secrets "$file_contents"; then
    exit 1
fi

echo "✅ All security checks passed!"

    # Format and lint checks
    deno task check:fmt
    if [ $? -ne 0 ]; then
      echo "Formatting failed. Please run 'deno fmt .' to fix."
      exit 1
    fi

    deno task check:lint
    if [ $? -ne 0 ]; then
      echo "Linting failed. Please fix lint errors."
      exit 1
    fi

    # Additional linting for staged server files (gradual approach)
    echo "🔍 Checking lint for staged server files..."
    STAGED_SERVER_FILES=$(git diff --cached --name-only | grep -E '^server/.*\.(ts|tsx)$' | tr '\n' ' ')

    if [ -n "$STAGED_SERVER_FILES" ]; then
        echo "📝 Staged server files: $STAGED_SERVER_FILES"
        
        # Run lint on each server file with relaxed rules
        LINT_FAILED=0
        for file in $STAGED_SERVER_FILES; do
            if [ -f "$file" ]; then
                LINT_OUTPUT=$(deno lint --quiet --rules-tags=recommended --rules-exclude=no-explicit-any,ban-untagged-todo "$file" 2>&1)
                if [ $? -ne 0 ]; then
                    echo "⚠️  Lint errors in $file:"
                    echo "$LINT_OUTPUT" | head -10
                    LINT_FAILED=1
                fi
            fi
        done
        
        if [ $LINT_FAILED -eq 1 ]; then
            echo ""
            echo "❌ Server file linting failed. Please fix critical lint errors."
            echo "💡 Tip: Common issues to fix:"
            echo "   - Unused variables (prefix with _ if intentional)"
            echo "   - Missing error handling"
            echo "   - Use const instead of let where possible"
            exit 1
        else
            echo "✅ Server files lint check passed"
        fi
    fi

    # TypeScript type checking for staged files only
    echo "🔍 Checking TypeScript types for staged files..."

    # Get staged TypeScript files
    STAGED_TS_FILES=$(git diff --cached --name-only | grep -E '\.(ts|tsx)$' | grep -v -E '(test|spec)\.(ts|tsx)$' | tr '\n' ' ')

    if [ -n "$STAGED_TS_FILES" ]; then
        echo "📝 Staged TypeScript files: $STAGED_TS_FILES"
        
        # Check types for staged files only (allow some errors for gradual improvement)
        echo "   Running incremental type check..."
        
        # Create a temporary file to capture type errors
        TYPE_ERRORS=$(deno check $STAGED_TS_FILES 2>&1 || true)
        
        if echo "$TYPE_ERRORS" | grep -q "error:"; then
            ERROR_COUNT=$(echo "$TYPE_ERRORS" | grep -c "error:" || echo "0")
            echo "⚠️  Found $ERROR_COUNT TypeScript errors in staged files:"
            echo "$TYPE_ERRORS" | head -20  # Show first 20 errors
            
            # Allow commit but warn about errors (gradual improvement approach)
            echo ""
            echo "📋 Type errors found but commit allowed (gradual improvement mode)"
            echo "💡 Consider fixing these errors when convenient:"
            echo "   - Run 'deno task analyze:types' to see all project errors"
            echo "   - Focus on MISSING_TYPE and IMPLICIT_ANY errors first"
            echo "   - Use 'deno check <file>' to check individual files"
            echo ""
            
            # Optionally fail on critical errors (uncomment to be stricter)
            # if [ $ERROR_COUNT -gt 10 ]; then
            #     echo "❌ Too many type errors ($ERROR_COUNT) - commit blocked"
            #     echo "   Please fix critical errors before committing"
            #     exit 1
            # fi
        else
            echo "✅ No TypeScript errors in staged files"
        fi
    else
        echo "📝 No TypeScript files staged for commit"
    fi

    # All good!
    echo "🎉 Pre-commit checks completed successfully"
    exit 0
fi
