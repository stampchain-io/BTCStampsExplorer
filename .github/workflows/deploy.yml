name: Code Quality
on:
  push:
    branches: [dev]
  pull_request:
    branches:
      - main
      - dev

jobs:
  quality-checks:
    name: Quality Checks
    runs-on: ubuntu-latest
    env:
      CSRF_SECRET_KEY: "12323"  # Placeholder key

    permissions:
      id-token: write # Needed for auth with Deno Deploy
      contents: read # Needed to clone the repository

    steps:
      - name: Clone repository
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Validate OpenAPI Schema
        run: npm run validate:ci

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.1.4

      - name: Install reviewdog
        uses: reviewdog/action-setup@v1
        with:
          reviewdog_version: latest

      # Formatting checks
      - name: Check formatting
        run: deno fmt --check

      - name: Run deno fmt with reviewdog
        run: |
          deno fmt --check | reviewdog -f=diff \
            -name="deno-fmt" \
            -reporter=github-pr-review \
            -filter-mode=added \
            -fail-level=any
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Linting checks
      - name: Check linting
        run: deno task check:lint

      - name: Run deno lint with reviewdog
        run: |
          deno lint --json | reviewdog -f=json \
            -name="deno-lint" \
            -reporter=github-pr-review \
            -filter-mode=added \
            -fail-level=error
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # - name: Type check
      #   run: deno task check:types

      - name: Build step
        run: deno task build

      # - name: Start Deno app
      #   run: deno run -A main.ts &
      #   env:
      #     PORT: 8000

      # - name: Wait for Deno app to be ready
      #   run: |
      #     echo "Waiting for Deno app to start..."
      #     sleep 10

      # - name: Install Dredd
      #   run: npm install -g dredd

      # - name: Run Dredd tests
      #   run: dredd
      #   env:
      #     DREDD_SERVER: http://localhost:8000