{
  "name": "btcstampsexplorer",
  "description": "This is the API for https://stampchain.io/ and the open source block explorer for Bitcoin Stamps.",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "validate:schema": "npx @redocly/cli@latest lint --config .redocly.yaml --format stylish ./schema.yml",
    "validate:ci": "npm run validate:schema",
    "test:api": "docker-compose -f docker-compose.test.yml run --rm dredd",
    "dredd": "docker-compose -f docker-compose.test.yml run --rm dredd",
    "prepare": "husky"
  },
  "dependencies": {
    "bitcoinjs-lib": "7.0.0-rc.0",
    "readable-stream": "4.3.0",
    "tiny-secp256k1": "^2.2.3"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "main": "swagger.js",
  "directories": {
    "lib": "lib",
    "test": "tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
