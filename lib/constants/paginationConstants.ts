/**
 * Pagination Constants for BTC Stamps Explorer
 * Runtime constants for pagination and loading states
 */

/**
 * Sort direction constants for queries and UI
 */
export enum SortDirection {
  ASC = "asc",
  DESC = "desc",
}

/**
 * Loading state constants for UI components
 */
export enum LoadingState {
  IDLE = "idle",
  LOADING = "loading",
  SUCCESS = "success",
  ERROR = "error",
  LOADING_MORE = "loading_more",
}
